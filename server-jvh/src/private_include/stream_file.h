
#ifndef JVH_FILE_STREAM_H
#define JVH_FILE_STREAM_H

#include "mqueue.h"
#include "stream.h"
#include <list>
#include <queue>

namespace jvh
{
   /* //! Childclass which encodes raw video
    class FileStream : public Stream
    {
    public:
        FileStream (std::string filepath);
        ~FileStream ();

        virtual std::shared_ptr<videostream::ToClient> stream_dequeue ();
    private:

    };*/
}

#endif // JVH_FILE_STREAM_H
